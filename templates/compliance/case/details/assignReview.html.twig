{{ form_start(assignReviewForm) }}
<div class="card mb-3">
    <div class="card-body bg-light">
        <div class="row">
            <div class="col-lg">
                {{ form_row(assignReviewForm.assignButton, { 'label': 'compliance.case.label.assign_review'|trans }) }}
            </div>
        </div>
    </div>
</div>
<div class="modal fade" id="assignModal" tabindex="-1" aria-hidden="true">
    <div class="modal-dialog modal-lg">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title">{{ 'compliance.case.title.assign_review'|trans }}</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body">
                <div class="row mb-3">
                    <div class="col-lg">
                        <span class="">{{ 'compliance.case.text.assign_review'|trans }}</span>
                    </div>
                </div>
                <div class="row">
                    <div class="col-lg">
                        {{ form_row(assignReviewForm.assignTo) }}
                    </div>
                </div>
                <div class="row">
                    <div class="col-lg">
                        {{ form_row(assignReviewForm.handler) }}
                    </div>
                </div>
            </div>
            <div class="modal-footer text-end">
                {{ form_row(assignReviewForm._token) }}
                {{ form_row(assignReviewForm.assignSubmit, { 'label': 'compliance.case.label.assign_case'|trans }) }}
            </div>
        </div>
    </div>
</div>
{{ form_end(assignReviewForm, {'render_rest': false}) }}

<script>
    const selectElement = document.getElementById("case_assign_handler");
    selectElement.setAttribute('disabled', 'disabled');
    selectElement.selectedIndex = 0;
    function assignerToggle() {
        let result = document.querySelector('input[name="case_assign[assignTo]"]:checked').value;
        if (result === "user") {
            selectElement.disabled = false;
        }
        if(result === "self"){
            selectElement.setAttribute('disabled', 'disabled');
            selectElement.selectedIndex = 0;
        }
    }
</script>
